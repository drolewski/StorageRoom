<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.drolewski.storageroom.activities">

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditObject"></activity>
        <activity android:name=".AddCategory" />
        <activity android:name=".EditBox" />
        <activity android:name=".BoxList" />
        <activity android:name=".AddBox" />
        <activity android:name=".AddSingleItem" />
        <activity android:name=".AddItem" />
        <activity
            android:name=".LocalizationList"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".MenuStart" />
        <activity
            android:name=".AddLocalization"
            android:label="@string/add_localization_title"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>